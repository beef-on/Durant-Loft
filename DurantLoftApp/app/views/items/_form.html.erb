<%= form_with(model: item, local: true) do |form| %>
  <% if item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :User_id, "Enter your id pharisee"%>
    <%= form.text_field :User_id, id: :item_User_id %>
  </div>

  <div class="field">
    <%= form.label :checkable, "For testing purposes" %>
    <%= form.check_box :checkable, id: :item_checkable %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
